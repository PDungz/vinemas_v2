import 'package:flutter/material.dart';

class AppColor {
  // Define all the colors here
  static const primaryColor = Color(0xFF1A2232);
  static const secondaryColor = Color(0xFF1F293D);
  static const accentColor = Color(0xFF253554);

  // Define all the button colors here
  static const buttonLinerOneColor = Color(0xFFFF8036);
  static const buttonLinerTwoColor = Color(0xFFFC6C19);

  // Define all the text colors here
  static const primaryTextColor = Color(0xFFFFFFFF);
  static const secondaryTextColor = Color(0xFF637394);

  // Define all the icon colors here
  static const primaryIconColor = Color(0xFF637394);

  // Define notification colors
  static const successColor = Color(0xFF28A745);
  static const errorColor = Color(0xFFFF4C4C);
  static const warningColor = Color(0xFFFFC107);
  static const infoColor = Color(0xFF17A2B8);
}
